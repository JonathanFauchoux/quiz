import clearFrameInterval from './_clearFrameInterval'
import clearFrameTimeout from './_clearFrameTimeout'
import setFrameInterval from './_setFrameInterval'
import setFrameTimeout from './_setFrameTimeout'
import wait from './_wait'

export {
  clearFrameInterval,
  clearFrameTimeout,
  setFrameInterval,
  setFrameTimeout,
  wait
}

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
  updatePageContent: ({ commit }, content) => {}
}

export default {
  state,
  getters,
  mutations,
  actions,
  namespaced: true
}
